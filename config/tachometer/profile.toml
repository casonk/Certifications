[repo]
name = "Certifications"
category = "doc-repos"
kind = "docs"

[paths]
disk_path = "."
profile_path = ".tachometer/profile.json"
summary_path = ".tachometer/summary.json"

[defaults]
label = "repo-snapshot"
# [notify] values are machine-local — do not commit real paths or contact identifiers here.
[notify]
shock_relay_root = ""
service = "signal"
target = ""
